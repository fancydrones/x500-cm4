name: Router image

on:
  push:
    branches: ['main']
    paths:
      - 'apps/router/**'

jobs:
  process-image:
    uses: ./.github/workflows/process-image-template.yaml
    with:
      platforms: linux/amd64,linux/arm64
      app_name: router
