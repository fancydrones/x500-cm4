apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    name: router-ex
  name: router-ex
  namespace: rpiuav
spec:
  replicas: 1
  selector:
    matchLabels:
      app: router-ex-replicaset
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: router-ex-replicaset
        app.kubernetes.io/name: router-ex-deployment
    spec:
      containers:
      - env:
        - name: RELEASE_NODE
          value: router_ex@router-ex
        - name: RELEASE_COOKIE
          value: router_ex_cookie
        - name: LOG_LEVEL
          value: info
        - name: ROUTER_CONFIG
          valueFrom:
            configMapKeyRef:
              key: ROUTER_CONFIG
              name: rpi4-config
        image: ghcr.io/fancydrones/x500-cm4/router-ex:20251024-a051572
        livenessProbe:
          exec:
            command:
            - pgrep
            - -f
            - beam.smp
          failureThreshold: 3
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
        name: router-ex
        ports:
        - containerPort: 5760
          name: tcp-mavlink
          protocol: TCP
        - containerPort: 14550
          name: udp-gcs
          protocol: UDP
        - containerPort: 14560
          name: udp-video0
          protocol: UDP
        - containerPort: 14561
          name: udp-video1
          protocol: UDP
        - containerPort: 14562
          name: udp-extra0
          protocol: UDP
        - containerPort: 14563
          name: udp-extra1
          protocol: UDP
        readinessProbe:
          exec:
            command:
            - pgrep
            - -f
            - beam.smp
          failureThreshold: 3
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
        resources:
          limits:
            cpu: "1.0"
            memory: 1Gi
          requests:
            cpu: "0.25"
            memory: 256Mi
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /dev/serial0
          name: serial0
      hostNetwork: true
      initContainers:
      - command:
        - sh
        - -c
        - echo "initContainer will delay startup by 20s to ensure serial device is
          ready" && sleep 20 && echo "exiting"
        image: busybox:1.34
        name: init-delay
      volumes:
      - hostPath:
          path: /dev/serial0
        name: serial0
