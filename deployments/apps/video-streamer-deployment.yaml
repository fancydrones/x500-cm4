apiVersion: v1
kind: Service
metadata:
  name: video-streamer-service
  namespace: rpiuav
spec:
  ports:
  - name: rtsp
    port: 8554
    protocol: TCP
    targetPort: 8554
  selector:
    app: video-streamer-replicaset
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: video-streamer-deployment
    name: video-streamer
  name: video-streamer
  namespace: rpiuav
spec:
  replicas: 1
  selector:
    matchLabels:
      app: video-streamer-replicaset
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: video-streamer-replicaset
        app.kubernetes.io/name: video-streamer-deployment
    spec:
      containers:
      - env:
        - name: STREAM_WIDTH
          value: "1280"
        - name: STREAM_HEIGHT
          value: "720"
        - name: STREAM_FPS
          value: "30"
        - name: CAMERA_HFLIP
          value: "true"
        - name: CAMERA_VFLIP
          value: "true"
        - name: RTSP_PORT
          value: "8554"
        - name: H264_PROFILE
          value: main
        - name: H264_LEVEL
          value: "4.1"
        image: ghcr.io/fancydrones/x500-cm4/video-streamer:20251024-b2edd89
        name: video-streamer
        ports:
        - containerPort: 8554
          name: rtsp
          protocol: TCP
        resources:
          limits:
            cpu: "3"
            memory: 2048Mi
          requests:
            cpu: "0.5"
            memory: 500Mi
        startupProbe:
          tcpSocket:
            port: 8554
          initialDelaySeconds: 5
          periodSeconds: 2
          failureThreshold: 15
        livenessProbe:
          tcpSocket:
            port: 8554
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /run/udev
          name: udev
          readOnly: true
        - mountPath: /dev
          name: dev
        - mountPath: /dev/shm
          name: temp
      hostNetwork: true
      volumes:
      - hostPath:
          path: /run/udev/
          type: Directory
        name: udev
      - hostPath:
          path: /dev
          type: Directory
        name: dev
      - emptyDir:
          medium: Memory
        name: temp
